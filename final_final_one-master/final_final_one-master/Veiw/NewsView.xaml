<UserControl x:Class="DataGridNamespace.Veiw.NewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataGridNamespace.Veiw"
             xmlns:models="clr-namespace:DataGridNamespace.Models"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <!-- FAB Style -->
        <Style x:Key="FabButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="56"/>
            <Setter Property="Height" Value="56"/>
            <Setter Property="Background" Value="#008080"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#008080"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
            <Setter Property="Margin" Value="0,0,24,24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="28" Effect="{DynamicResource DropShadowEffect}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#006666"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- Delete Icon Button Style -->
        <Style x:Key="DeleteIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFF0F0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <DropShadowEffect x:Key="DropShadowEffect" BlurRadius="8" ShadowDepth="2" Color="#44000000"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="News and Announcements" 
                   FontSize="28"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>
        <!-- News Items -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="NewsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:NewsItem}">
                        <Border BorderThickness="1" 
                                BorderBrush="#E0E6ED" 
                                Background="White" 
                                CornerRadius="8" 
                                Margin="0,0,0,15" 
                                Padding="15">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <!-- Delete Button (Admin only) -->
                                <Button Style="{StaticResource DeleteIconButtonStyle}"
                                        Visibility="{Binding DataContext.IsAdmin, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibilityConverter}}"
                                        Tag="{Binding NewsItemID}"
                                        Click="DeleteNewsButton_Click">
                                    <iconPacks:PackIconMaterial Kind="TrashCanOutline" Width="20" Height="20" Foreground="#E53935"/>
                                </Button>
                                <StackPanel Grid.Row="1">
                                    <!-- Title -->
                                    <TextBlock Text="{Binding Title}" 
                                               FontSize="18" 
                                               FontWeight="SemiBold" 
                                               Foreground="#006666"/>
                                    <!-- Metadata: Date and Author -->
                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                                        <TextBlock Text="Published: " 
                                                   FontSize="12" 
                                                   Foreground="#666666"/>
                                        <TextBlock Text="{Binding PublishedDate, StringFormat='{}{0:dddd, MMMM dd, yyyy hh:mm tt}'}" 
                                                   FontSize="12" 
                                                   Foreground="#666666"/>
                                        <TextBlock Text=" by " 
                                                   FontSize="12" 
                                                   Foreground="#666666"
                                                   Visibility="{Binding Author, Converter={StaticResource NullOrEmptyToVisibilityConverter}}"/>
                                        <TextBlock Text="{Binding Author}" 
                                                   FontSize="12" 
                                                   Foreground="#666666"
                                                   Visibility="{Binding Author, Converter={StaticResource NullOrEmptyToVisibilityConverter}}"/>
                                    </StackPanel>
                                    <!-- Content -->
                                    <TextBlock Text="{Binding Content}" 
                                               TextWrapping="Wrap" 
                                               FontSize="14" 
                                               Foreground="#333333"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <!-- Floating Add News Button (Admin only) -->
        <Button x:Name="AddNewsButton"
                Style="{StaticResource FabButtonStyle}"
                Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVisibilityConverter}}"
                Click="AddNewsButton_Click"
                ToolTip="Add News">
            <iconPacks:PackIconMaterial Kind="Plus" Width="32" Height="32"/>
        </Button>
    </Grid>
</UserControl> 