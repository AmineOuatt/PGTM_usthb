<UserControl x:Class="DataGridNamespace.Admin.DemandsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataGridNamespace.Admin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#008080" Padding="12,10" CornerRadius="8,8,0,0">
            <TextBlock Text="Pending Theses" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="White"/>
        </Border>

        <!-- DataGrid -->
        <DataGrid x:Name="PendingThesesGrid" 
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="12"
                  BorderThickness="1"
                  BorderBrush="#E5E7EB">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Titre}" Width="*"/>
                <DataGridTextColumn Header="Author" Binding="{Binding Auteur}" Width="150"/>
                <DataGridTextColumn Header="Specialty" Binding="{Binding Speciality}" Width="100"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="Year" Binding="{Binding Annee, StringFormat=yyyy}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl> 